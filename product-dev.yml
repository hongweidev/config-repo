spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/SpringCloud_sell?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&useSSL=false
    username: admin
    password: admin
mybatis:
  mapperLocations: classpath:mapper/*Mapper.xml
  typeAliasesPackage: com.example.product.domain
